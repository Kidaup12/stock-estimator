---
phase: 01-boot-determinism-cleanup
plan: complete
total_tasks: 9
status: phase_complete_awaiting_phase_2_or_3_decision
last_updated: 2026-05-29T23:00:05.377Z
---

<current_state>
**Phase 1 is DONE.** All 3 plans (Postgres swap → forecast determinism → vitest harness) shipped, 18 commits on `main` of the local clone at `C:/Users/ROY/Documents/wezesha/stock-estimator/`, NOT yet pushed to `Kidaup12/stock-estimator` upstream. Every FND requirement (FND-01..07) is verified live on Supabase Postgres.

The pause point is **strategic, not technical**. The next step is a decision between:
- **Phase 2** (Supabase Auth + multi-tenant — the canonical roadmap order; unblocks every subsequent phase)
- **Phase 3** (real Shopify ingest, single-tenant — faster path to a "Mary sees her own data" demo)

Roy is pausing to decide WITH Anjay rather than guess. The localhost:3082 dev server is still running in background (PID `b2jqbb3dj`); harmless to leave OR kill.
</current_state>

<completed_work>

## Wave 1 — Plan 01-01 (commits `54fb50d`..`151d887`)
Postgres swap + schema deltas + dev.db scrub + README rewrite. Live on Supabase eu-central-1 Frankfurt.

## Wave 2 — Plan 01-02 (commits `09e9b59`..`ce99b83`)
- `lib/forecast/{rng,rng-constants,abc,reorder}.ts` created
- 21 `Math.random()` sites replaced with seeded mulberry32 (Fisher-Yates fix in synth)
- `app/api/orders/[id]/approve/route.ts` rewritten as transactional + idempotent onOrder.increment (codex catch #1)
- forecast/run + scripts/run-forecasts wired to helpers, deleteMany removed, single forecastRunId per batch
- `app/api/forecast/route.ts` dashboard query now pins to latest forecastRunId (codex catch #3)

## Wave 3 — Plan 01-03 (commits `680fb96`..`ae47bbc`)
- vitest installed, 3 colocated test files for rng/abc/reorder
- `scripts/check-determinism.ts` + npm scripts: `test`, `test:watch`, `check:determinism`, `check:phase1`
- 22/22 tests passing; check:determinism PASS

## Live Verification (via Playwright + curl)
- Run 1 + Run 2 sample[5] critical predictions = byte-identical (FND-02 ✓)
- Approve Glow Bundle (orderId `cmprfz8g1059pv8x8559f6vjz`, productId `cmprdm77m000wv8q8blk966fp`) → onOrder 0→65 transactionally
- Second approve POST → `{ alreadyApproved: true }`, no double-increment (codex #1 ✓)
- Run 3 → Glow Bundle's recommendedQty = 0 (was 65); dashboard Reorder count 109→108 (FND-04 ✓)
- Screenshots committed: `01-01-dashboard-proof.png`, `01-02-dashboard-post-approve.png`

</completed_work>

<remaining_work>

## Outside this phase

- [ ] **Roy + Anjay**: Phase 2 vs Phase 3 ordering decision (strategic, not technical)
- [ ] **Roy** (optional): Screen-share `localhost:3082` to Anjay as Level-1 demo
- [ ] **Roy** (optional): `git push origin main` to ship 19 commits to Kidaup12 upstream
- [ ] **Phase 2** (if chosen): Supabase Auth + path-based `/shop/[slug]/` routing + `requireTenant()` chokepoint + 12 `prisma.tenant.findFirst()` removals + ESLint guard rule. AUTH-01..05 + TNT-01..08 (13 requirements)
- [ ] **Phase 3** (if chosen as shortcut): Real Shopify OAuth + Admin API ingest + webhooks + nightly reconcile + Odoo. SHOP-01..09 + ODOO-01..05 (14 requirements)

</remaining_work>

<decisions_made>

- **Supabase project**: Roy's personal account (`lkkljxvuhkaydhffpaix`, eu-central-1) for dev. Anjay's SimplyDone account when prod ships. Transfer = re-run migrations, no data loss (dev data is synthetic).
- **Connection**: Session Pooler (port 5432, IPv4-proxied) for both DATABASE_URL and DIRECT_URL. Direct connection (db.<ref>.supabase.co) is IPv6-only on free tier; Kenyan ISPs are IPv4. The Session Pooler works for both runtime AND prisma migrate, eliminating the need for the Transaction Pooler at this stage.
- **Dev port**: 3082 (baked into `package.json` "dev" script). 3000 was occupied on Roy's machine.
- **SQLite baseline (§A in original RUNBOOK)**: Skipped. Codex flagged it as belt-and-suspenders; §D Postgres boot covers the same FND-01 ground without the rollback dance.
- **Codex's 6 review findings**: Applied inline via Edit tool, not via `/gsd:plan-phase 1 --reviews`. Surgical patches that preserved plan structure.
- **`forecastRunId @default(cuid())`** at schema level: Migration-safe per codex #4. Default only fires for orphan writes; route still supplies a single batch ID per run.
- **Plan 03 `assignAbc` test fixture off-by-one**: The PLAN's example expected single-product → A but the helper returns C (cumulative-after-add semantics). Executor rewrote the test to lock the helper's actual behavior (which Plan 02 verified live). Helper correct; PLAN's example was wrong.

</decisions_made>

<blockers>

- **Phase 2 vs Phase 3 ordering** — Anjay's call. Default = Phase 2 per ROADMAP.md if no input. Phase 3 single-tenant shortcut is defensible if Anjay wants to show Mary real data fast.
- **No other blockers.** All technical work for Phase 1 is verified-green.

</blockers>

<context>

Phase 1 was the **boring foundation** nobody pays for — turning a single-tenant SQLite demo into a Postgres-backed app with deterministic forecasts, real reorder math, and a working approve feedback loop. Three things make this state worth preserving:

1. **18 commits** are in the local clone but NOT in `Kidaup12/stock-estimator` upstream. Anjay can't see Phase 1 until Roy pushes.
2. **Dev server is alive** at localhost:3082 (background PID `b2jqbb3dj`). Anyone in this conversation tab can drive it via Playwright. New tab: kill it and restart with `npm run dev`.
3. **`.env` is gitignored**. New clone of this repo would need:
   - DATABASE_URL = `postgresql://postgres.lkkljxvuhkaydhffpaix:%40Wezesha254@aws-1-eu-central-1.pooler.supabase.com:5432/postgres`
   - DIRECT_URL = same
   - (Password `@Wezesha254` URL-encoded as `%40Wezesha254`)

**Roy's session preferences** (apply to future sessions on this project):
- Grade-9 explanations when concepts are technical
- AskUserQuestion with concrete options + recommendation labeled "(Recommended)"
- CLI-first execution per memory `feedback_cli_first_execution`
- Ask before destructive operations (Prisma's AI-safety guard already enforces this for migrations)
- Codex (`codex exec`) as a second-opinion reviewer — proven valuable on Phase 1 (caught 6 real issues plan-checker missed)
- Port 3082, not 3000

**The strategic question for Phase 2 vs Phase 3:**

> Strictly speaking, Phase 2 (auth) is the dependency — the roadmap says auth → real data. But for a *Beauty Square only* pilot, you could ship Phase 3 against the single-tenant baseline first, prove the value, then retrofit auth before client #2 lands.

The "right" answer depends on whether Anjay wants (a) a polished product to show 5 prospects OR (b) one extraordinary demo for Mary that closes the contract.

</context>

<next_action>

In a fresh tab from `wezesha/stock-estimator/`:

```bash
# 1. Resume context
/gsd:resume-work

# 2. (If dev server was killed) restart it
npm run dev   # binds to :3082

# 3. Either continue with the canonical roadmap...
/gsd:discuss-phase 2 --auto

# OR take the demo shortcut...
/gsd:discuss-phase 3 --auto
```

If pausing for >24h, kill the dev server first (close any terminal still running `npm run dev`, or use Task Manager → end Node processes).

To push Phase 1 to Anjay's view:
```bash
git push origin main   # 19 commits land upstream
```

</next_action>

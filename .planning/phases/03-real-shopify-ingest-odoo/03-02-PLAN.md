---
phase: 03-real-shopify-ingest-odoo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - .env.example
  - lib/shopify/shopify.ts
  - lib/auth/webhook-context.ts
  - app/api/shopify/auth/route.ts
  - app/api/shopify/callback/route.ts
  - app/api/shopify/connection/route.ts
  - app/shop/[slug]/settings/page.tsx
autonomous: false
requirements: [SHOP-01, SHOP-02]
user_setup:
  - service: shopify
    why: "Public OAuth app — Wezesha cannot create the Partner app or mint API credentials; only the Shopify account owner can (D-01)."
    env_vars:
      - name: SHOPIFY_API_KEY
        source: "Shopify Partner Dashboard -> Apps -> (your app) -> Client ID"
      - name: SHOPIFY_API_SECRET
        source: "Shopify Partner Dashboard -> Apps -> (your app) -> Client secret"
      - name: SHOPIFY_APP_HOST
        source: "The public base URL of this app (Vercel prod URL, or the dev tunnel URL if a tunnel is needed for OAuth — see OQ1)"
    dashboard_config:
      - task: "Create a public app in the Partner Dashboard; set App URL + Allowed redirection URL to {SHOPIFY_APP_HOST}/api/shopify/callback; request scopes read_products,read_inventory,read_orders,read_locations"
        location: "Shopify Partner Dashboard -> Apps -> Create app -> Configuration"
must_haves:
  truths:
    - "Owner clicks Connect Shopify on /shop/[slug]/settings, completes the Shopify consent screen, and returns to the dashboard"
    - "After connect, an offline access token is stored ENCRYPTED on ShopifyConnection (ciphertext, not the raw shpat_ token) and Tenant.shopifyDomain is set to the *.myshopify.com shop"
    - "The settings page shows connected/not-connected status for the tenant"
  artifacts:
    - path: "lib/shopify/shopify.ts"
      provides: "Configured shopifyApi singleton pinned to ApiVersion.April26, offline mode, isEmbeddedApp:false"
      contains: "ApiVersion.April26"
    - path: "app/api/shopify/auth/route.ts"
      provides: "OAuth begin — shopify.auth.begin offline, carries tenant slug in state"
      exports: ["GET"]
    - path: "app/api/shopify/callback/route.ts"
      provides: "OAuth callback — exchanges code for offline token, encrypts, upserts ShopifyConnection, sets Tenant.shopifyDomain"
      exports: ["GET"]
  key_links:
    - from: "app/api/shopify/callback/route.ts"
      to: "lib/crypto/encryption.ts encrypt()"
      via: "encrypt(session.accessToken) before prisma write"
      pattern: "encrypt\\("
    - from: "app/api/shopify/callback/route.ts"
      to: "ShopifyConnection + Tenant.shopifyDomain"
      via: "prisma.shopifyConnection.upsert + tenant.update shopifyDomain"
      pattern: "shopifyConnection\\.upsert"
    - from: "app/shop/[slug]/settings/page.tsx"
      to: "/api/shopify/auth?shop=..."
      via: "Connect Shopify button navigates to the begin route"
      pattern: "/api/shopify/auth"
---

<objective>
Build the per-tenant Shopify OAuth installation flow (SHOP-01): the configured `@shopify/shopify-api@13` singleton, the begin/callback route handlers that exchange the auth code for an OFFLINE access token, encrypt it (SHOP-02), and persist it on `ShopifyConnection` bound to BOTH the Shopify shop and the Wezesha tenant. Add the "Connect Shopify" button + connection status to the settings page, and fill in the Phase-2 `resolveTenantByDomain` placeholder to use `findUnique` (D-07).

Purpose: This produces the token every downstream plan needs. Backfill (Plan 03), webhooks (Plan 04), and reconcile (Plan 05) all reconstruct a Shopify session from the encrypted `ShopifyConnection.accessToken` this plan writes.
Output: A working Connect Shopify -> consent -> encrypted-token-stored round trip.

**Human checkpoint:** Roy creates the Shopify Partner app and provides SHOPIFY_API_KEY / SHOPIFY_API_SECRET / SHOPIFY_APP_HOST + configures the redirect URL and scopes. Open question OQ1 (does localhost:3082 work as a redirect, or is a tunnel required) is resolved during the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-shopify-ingest-odoo/03-CONTEXT.md
@.planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md
@.planning/phases/03-real-shopify-ingest-odoo/03-01-SUMMARY.md

<interfaces>
<!-- From lib/crypto/encryption.ts (Plan 01) — use directly, do not re-implement -->
export function encrypt(plaintext: string): string;
export function decrypt(payload: string): string;

<!-- From lib/auth/context.ts (Phase 2) — resolve the tenant in the callback by the slug carried through OAuth state -->
export async function requireTenant(slugArg?: string): Promise<{ tenant: { id: string; slug: string; ... }, membership, userId: string }>;
// throws TenantError(401|403|404)
// route-wrapper variant: requireTenantOrResponse(slugArg?) returns TenantContext | NextResponse

<!-- From lib/auth/webhook-context.ts (Phase 2 placeholder you are filling) -->
export async function resolveTenantByDomain(domain: string); // currently findFirst; switch to findUnique (D-07, now that shopifyDomain is @unique)

<!-- Prisma models added in Plan 01 -->
// ShopifyConnection { tenantId @unique, shopDomain @unique, accessToken (ciphertext), scope, installedAt, uninstalledAt? }
// Tenant.shopifyDomain String? @unique
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Roy creates the Shopify Partner app + provides API credentials</name>
  <what-built>Nothing yet — this is the unavoidable manual prerequisite. The Partner app + API credentials can only be created by the Shopify account owner; Wezesha cannot mint them via CLI/API.</what-built>
  <how-to-verify>
    Roy completes these in the Shopify Partner Dashboard, then pastes the values into `.env`:
    1. Create a PUBLIC app (Partner Dashboard -> Apps -> Create app -> Public). Copy the Client ID -> `SHOPIFY_API_KEY`, Client secret -> `SHOPIFY_API_SECRET`.
    2. Decide the App host for dev (OQ1): try `http://localhost:3082` as the App URL + Allowed redirection URL `http://localhost:3082/api/shopify/callback`. If Shopify rejects a non-HTTPS/localhost redirect for a public app, stand up a tunnel (`shopify app dev` or `cloudflared tunnel`) and use that HTTPS URL instead. Set `SHOPIFY_APP_HOST` to whichever base URL works (no trailing slash, no path).
    3. Set Allowed redirection URL(s) to `{SHOPIFY_APP_HOST}/api/shopify/callback`.
    4. Set the requested scopes to exactly `read_products,read_inventory,read_orders,read_locations` (the full day-1 set so no re-install is needed later — RESEARCH §6).
    5. Confirm `SHOPIFY_API_KEY`, `SHOPIFY_API_SECRET`, `SHOPIFY_APP_HOST` are all set in `.env`.
    Record the OQ1 outcome (localhost vs tunnel) so the SUMMARY documents it.
  </how-to-verify>
  <resume-signal>Type "creds set" once SHOPIFY_API_KEY / SHOPIFY_API_SECRET / SHOPIFY_APP_HOST are in .env and the redirect URL + scopes are configured. Note whether localhost or a tunnel was used.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @shopify/shopify-api@13 + session-storage-prisma@9, update .env.example, build the shopify singleton</name>
  <files>package.json, .env.example, lib/shopify/shopify.ts</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Standard Stack" (pinned versions + install command) + §"Pattern 1 — Shopify client singleton" lines 40-54 (copy the shopifyApi config exactly)
    - 03-RESEARCH.md line 17 + Open Question #2: confirm ApiVersion.April26 is still the current stable at execute time via `npm view @shopify/shopify-api` / Shopify changelog; if 2026-07 has shipped, pin ApiVersion.July26 instead and note it
    - .env.example lines 27-31 (Phase 3 Shopify block — SHOPIFY_API_KEY/SECRET exist; SHOPIFY_SCOPES exists but lists write_draft_orders which is NOT needed for read-only ingest — correct it; SHOPIFY_APP_URL exists but the code expects SHOPIFY_APP_HOST — reconcile the name)
  </read_first>
  <action>
    1. Install the pinned versions (CLI-first): `npm install @shopify/shopify-api@13 @shopify/shopify-app-session-storage-prisma@9`. Confirm package.json now lists `"@shopify/shopify-api": "^13...."` and `"@shopify/shopify-app-session-storage-prisma": "^9...."`.

    2. Update `.env.example` Phase 3 Shopify block:
       - Keep `SHOPIFY_API_KEY=""`, `SHOPIFY_API_SECRET=""`.
       - Change `SHOPIFY_SCOPES` default to `"read_products,read_inventory,read_orders,read_locations"` (read-only ingest set; drop write_draft_orders — Phase 3 does not create draft orders).
       - Rename `SHOPIFY_APP_URL` -> `SHOPIFY_APP_HOST` with a comment: "Public base URL (Vercel prod, or dev tunnel). No protocol stripping needed in .env; the code strips it. No trailing slash."
       - Add `CRON_SECRET=""` with comment "Bearer token guarding /api/cron/* (Plan 05). Generate: openssl rand -base64 32" (added here so the env contract is complete; Plan 05 consumes it).

    3. Create `lib/shopify/shopify.ts` exactly per RESEARCH Pattern 1:
       ```ts
       import "@shopify/shopify-api/adapters/web-api";
       import { shopifyApi, ApiVersion } from "@shopify/shopify-api";

       export const shopify = shopifyApi({
         apiKey: process.env.SHOPIFY_API_KEY!,
         apiSecretKey: process.env.SHOPIFY_API_SECRET!,
         scopes: (process.env.SHOPIFY_SCOPES ?? "read_products,read_inventory,read_orders,read_locations").split(","),
         hostName: process.env.SHOPIFY_APP_HOST!.replace(/^https?:\/\//, ""),
         apiVersion: ApiVersion.April26,
         isEmbeddedApp: false,
       });
       ```
       The `import "@shopify/shopify-api/adapters/web-api";` line is REQUIRED — Next route handlers use Web `Request`/`Response`, not Node http. Keep it as the first import.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "lib/shopify/shopify.ts" || echo "shopify singleton typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@shopify/shopify-api" package.json` matches and `grep "shopify-app-session-storage-prisma" package.json` matches
    - `grep "ApiVersion.April26" lib/shopify/shopify.ts` matches (pinned, NOT LATEST_API_VERSION)
    - `grep "adapters/web-api" lib/shopify/shopify.ts` matches (web runtime adapter present)
    - `grep "isEmbeddedApp: false" lib/shopify/shopify.ts` matches
    - `grep "SHOPIFY_APP_HOST" .env.example` matches and `grep "read_locations" .env.example` matches and `grep "CRON_SECRET" .env.example` matches
    - `grep "LATEST_API_VERSION" lib/shopify/shopify.ts` returns NOTHING
  </acceptance_criteria>
  <done>Pinned deps installed; singleton configured with April26 + offline + web-api adapter; .env.example documents SHOPIFY_APP_HOST, the read-only scopes, and CRON_SECRET.</done>
</task>

<task type="auto">
  <name>Task 2: OAuth begin + callback routes (offline token, encrypted, tenant-bound) + connection status route</name>
  <files>app/api/shopify/auth/route.ts, app/api/shopify/callback/route.ts, app/api/shopify/connection/route.ts, lib/auth/webhook-context.ts</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Pattern 2 — OAuth offline" lines 56-59 (begin/callback shapes, tenant binding via state, set Tenant.shopifyDomain = session.shop) + Open Question #2 (confirm v13 auth.begin/callback exact return shape from the v13 README — a 10-min spike before coding the routes)
    - lib/auth/context.ts (requireTenant(slugArg) signature — resolve the tenant in the callback by the slug carried in OAuth state) + lib/auth/route-wrapper.ts (requireTenantOrResponse pattern for the connection status route)
    - lib/auth/webhook-context.ts (the resolveTenantByDomain placeholder — switch findFirst -> findUnique, remove the TODO)
    - lib/crypto/encryption.ts (encrypt — wrap session.accessToken before persisting)
    - CLAUDE.md (Next 16 async params + API-route conventions; Zod for any body validation)
  </read_first>
  <action>
    Spike first (OQ2): open the installed `@shopify/shopify-api@13` README / types to confirm the exact `shopify.auth.begin(...)` and `shopify.auth.callback(...)` call signatures and return shapes under the web-api adapter (v13 returns/edits a `Response`). Build the routes to match the confirmed signature; the shapes below are the RESEARCH sketch.

    1. `app/api/shopify/auth/route.ts` (GET): read `?shop=<store>.myshopify.com` and `?slug=<tenant-slug>` from the URL. Call `await shopify.auth.begin({ shop, callbackPath: "/api/shopify/callback", isOnline: false, rawRequest: req })`. Carry the tenant slug through OAuth `state` (so the callback knows which Wezesha tenant to bind) — if v13's begin does not expose a state hook, set a short-lived signed cookie `shopify_oauth_slug` instead. Return the Response the adapter produces (the redirect to Shopify's consent screen).

    2. `app/api/shopify/callback/route.ts` (GET): `const { session } = await shopify.auth.callback({ rawRequest: req });`. `session.accessToken` is the OFFLINE token (because isOnline:false). Recover the tenant slug from state/cookie and resolve `const { tenant } = await requireTenant(slug)` (binds the token to the authenticated Wezesha tenant — do NOT trust the slug without the membership check). Then:
       - `await prisma.shopifyConnection.upsert({ where: { tenantId: tenant.id }, create: { tenantId: tenant.id, shopDomain: session.shop, accessToken: encrypt(session.accessToken), scope: session.scope ?? "" }, update: { shopDomain: session.shop, accessToken: encrypt(session.accessToken), scope: session.scope ?? "", uninstalledAt: null } })`.
       - `await prisma.tenant.update({ where: { id: tenant.id }, data: { shopifyDomain: session.shop } })` (D-07 — enables webhook resolution by domain).
       - Redirect to `/shop/${slug}/settings?connected=1`.
       - Leave a clearly-commented hook for Plan 03 (kick off backfill) and Plan 04 (register webhooks) — those are implemented in later plans; do NOT block this redirect on them.

    3. `app/api/shopify/connection/route.ts` (GET): use `requireTenantOrResponse()`; return `{ connected: boolean, shopDomain: string | null, installedAt: Date | null }` derived from `prisma.shopifyConnection.findUnique({ where: { tenantId: tenant.id } })` (connected = row exists AND uninstalledAt is null). NEVER return the token. This feeds the settings status UI.

    4. `lib/auth/webhook-context.ts`: change `resolveTenantByDomain` from `prisma.tenant.findFirst({ where: { shopifyDomain: domain } })` to `prisma.tenant.findUnique({ where: { shopifyDomain: domain } })` and delete the `TODO(Phase 3)` comment (the @unique constraint now exists). Leave the file's allow-list header comment intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "app/api/shopify|webhook-context" || echo "shopify oauth routes typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isOnline: false" app/api/shopify/auth/route.ts` matches (OFFLINE token)
    - `grep "encrypt(" app/api/shopify/callback/route.ts` matches (token encrypted before persist)
    - `grep "shopifyConnection.upsert" app/api/shopify/callback/route.ts` matches and `grep "shopifyDomain: session.shop" app/api/shopify/callback/route.ts` matches
    - `grep "requireTenant" app/api/shopify/callback/route.ts` matches (token bound to the authorized tenant, slug not blindly trusted)
    - `grep "findUnique" lib/auth/webhook-context.ts` matches and `grep "findFirst" lib/auth/webhook-context.ts` returns NOTHING
    - `grep -i "TODO(Phase 3)" lib/auth/webhook-context.ts` returns NOTHING
    - `npx tsc --noEmit` reports no errors in the new shopify route files
  </acceptance_criteria>
  <done>begin redirects to Shopify; callback exchanges an offline token, encrypts it, upserts ShopifyConnection, sets Tenant.shopifyDomain, redirects to settings; connection route reports status without leaking the token; webhook resolver uses findUnique.</done>
</task>

<task type="auto">
  <name>Task 3: Connect Shopify button + connection status on the settings page</name>
  <files>app/shop/[slug]/settings/page.tsx</files>
  <read_first>
    - app/shop/[slug]/settings/page.tsx (existing "Shopify connection" Section ~lines 255-280 — ADD the Connect button + status NEXT TO the existing form; preserve the existing mock-mode form and the Section/Field/ResultBanner helpers; do not delete the monthly-context or catalogue sections)
    - lib/api-fetch.ts (apiFetch(slug, path, init) helper used throughout this page — use it to call /api/shopify/connection)
    - app/api/shopify/connection/route.ts (the status shape { connected, shopDomain, installedAt } from Task 2)
  </read_first>
  <action>
    In the existing "Shopify connection" Section of `app/shop/[slug]/settings/page.tsx`:
    1. On mount (extend the existing `load()` or add a small effect), call `apiFetch(slug, "/api/shopify/connection")` and store `{ connected, shopDomain }` in state.
    2. Add a primary "Connect Shopify" button. Its click handler prompts for / reads the merchant's `*.myshopify.com` domain (a small input, default empty) and navigates the browser to `/api/shopify/auth?shop=${encodeURIComponent(myshopifyDomain)}&slug=${slug}` (a full-page navigation, NOT apiFetch — OAuth needs a top-level redirect). Use `window.location.href = ...`.
    3. Render status: when `connected`, show "Connected to {shopDomain}" with a success ResultBanner; when not, show the Connect button. If the URL has `?connected=1` (post-callback redirect), surface a "Shopify connected" success banner.
    4. Keep the existing mock-mode domain/token form visible but label the new OAuth path as the real one (a one-line note: "Connect Shopify (OAuth) is the real integration; the fields below are legacy mock mode.").
    This task is UI wiring only — verified by the human checkpoint that follows.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "settings/page" || echo "settings page typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/api/shopify/auth" app/shop/[slug]/settings/page.tsx` matches (button kicks off OAuth begin)
    - `grep "/api/shopify/connection" app/shop/[slug]/settings/page.tsx` matches (status fetched)
    - `grep -i "Connect Shopify" app/shop/[slug]/settings/page.tsx` matches
    - `grep "window.location" app/shop/[slug]/settings/page.tsx` matches (full-page redirect for OAuth, not apiFetch)
    - `npx tsc --noEmit` reports no errors in the settings page
  </acceptance_criteria>
  <done>Settings page shows a Connect Shopify button that starts OAuth via a top-level redirect and renders the tenant's connection status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the OAuth round trip + encrypted token at rest</name>
  <what-built>The full OAuth round trip: Connect Shopify button -> /api/shopify/auth -> Shopify consent -> /api/shopify/callback -> encrypted token on ShopifyConnection -> redirect to settings.</what-built>
  <how-to-verify>
    With the Partner app creds in .env and the dev server (or tunnel target) running on :3082:
    1. Visit `/shop/beauty-square/settings`, click "Connect Shopify", enter Beauty Square's `*.myshopify.com` domain, proceed.
    2. Confirm you land on Shopify's consent screen, approve, and get redirected back to `/shop/beauty-square/settings?connected=1` showing "Connected to <shop>.myshopify.com".
    3. Verify the token is encrypted at rest, NOT plaintext, by running:
       `npx tsx -e "import {prisma} from './lib/prisma'; prisma.shopifyConnection.findFirst().then(c=>{console.log('shop',c?.shopDomain,'tokenStartsWithShpat',c?.accessToken?.startsWith('shpat_'),'tokenLen',c?.accessToken?.length); process.exit(0)})"`
       Expect `tokenStartsWithShpat=false` (it is ciphertext) and a non-trivial token length, and a real `*.myshopify.com` shopDomain.
    4. Verify Tenant.shopifyDomain was set: `npx tsx -e "import {prisma} from './lib/prisma'; prisma.tenant.findFirst({where:{slug:'beauty-square'}}).then(t=>{console.log('domain',t?.shopifyDomain); process.exit(0)})"` prints the *.myshopify.com domain.
    If OAuth fails on a localhost redirect, switch to the tunnel URL (OQ1) and retry.
  </how-to-verify>
  <resume-signal>Type "connected" once the round trip completes and the stored token is confirmed ciphertext (not shpat_). Report the OQ1 outcome (localhost vs tunnel) for the SUMMARY.</resume-signal>
</task>

</tasks>

<verification>
- OAuth round trip completes against the real Beauty Square store.
- ShopifyConnection.accessToken is ciphertext (does not start with `shpat_`); Tenant.shopifyDomain set to the *.myshopify.com shop (SHOP-01, SHOP-02).
- resolveTenantByDomain uses findUnique (D-07) and typechecks.
</verification>

<success_criteria>
- Connect Shopify -> consent -> callback -> encrypted offline token stored on ShopifyConnection, tenant-bound.
- Settings page reports connection status without leaking the token.
- ApiVersion pinned to April26; web-api adapter present; scopes are the read-only day-1 set.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-shopify-ingest-odoo/03-02-SUMMARY.md` (record the OQ1 localhost-vs-tunnel outcome and the confirmed v13 auth.begin/callback signature for downstream plans).
</output>

---
phase: 03-real-shopify-ingest-odoo
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/shopify/bulk.ts
  - lib/shopify/jsonl.ts
  - lib/shopify/jsonl.test.ts
  - lib/shopify/ingest.ts
  - app/api/shopify/backfill/route.ts
  - app/shop/[slug]/settings/page.tsx
  - app/api/shop/test/route.ts
  - lib/shopify/client.ts
  - eslint.config.mjs
autonomous: false
requirements: [SHOP-03, SHOP-04, SHOP-08]
must_haves:
  truths:
    - "On first connect, 365 days of orders + all products/variants + on_hand inventory backfill via Bulk Operations and render in the dashboard"
    - "Location is a first-class entity with isPrimary set to the first active location; InventoryLevel.onHand holds the on_hand quantity (not available)"
    - "The synthetic->real swap is GUARDED: a dry-run count + explicit owner confirmation precede any deleteMany, and only Beauty Square's synthetic Product/SalesHistory/Prediction/Order are cleared (Supplier/Promo/MonthlyContext preserved)"
    - "The mock ShopifyClient is deleted and its eslint.config.mjs allow-list entry removed"
  artifacts:
    - path: "lib/shopify/jsonl.ts"
      provides: "Streaming JSONL parser that reassembles __parentId nesting"
      contains: "__parentId"
    - path: "lib/shopify/ingest.ts"
      provides: "Tenant-scoped upsert mappers: products/variants, locations, inventory on_hand, orders->SalesHistory (reused by webhooks Plan 04 + reconcile Plan 05)"
      exports: ["upsertProductFromShopify", "upsertLocationFromShopify", "upsertInventoryLevel", "upsertOrderAsSales"]
    - path: "lib/shopify/bulk.ts"
      provides: "bulkOperationRunQuery launch + currentBulkOperation poll + JSONL fetch for orders/products/inventory"
      contains: "bulkOperationRunQuery"
  key_links:
    - from: "lib/shopify/bulk.ts inventory query"
      to: "Shopify on_hand quantity"
      via: 'quantities(names: ["on_hand"])'
      pattern: "quantities\\(names:"
    - from: "app/api/shopify/backfill/route.ts cutover"
      to: "prisma deleteMany (guarded)"
      via: "dry-run count + confirm flag before delete, tenant-scoped"
      pattern: "deleteMany"
    - from: "lib/shopify/ingest.ts upsertProductFromShopify"
      to: "Product @@unique([tenantId, shopifyProductId])"
      via: "prisma.product.upsert on the composite key"
      pattern: "tenantId_shopifyProductId"
---

<objective>
The live acceptance for Phase 3: pull Beauty Square's REAL catalog, 365 days of orders, and `on_hand` inventory via GraphQL Bulk Operations (SHOP-03, SHOP-04), make `Location`/`InventoryLevel` first-class (SHOP-08), and perform the GUARDED synthetic->real cutover (D-13) so the dashboard renders real data. Also delete the mock `ShopifyClient` and remove its tenant-safety ESLint allow-list entry now that real ingest exists.

Purpose: This plan owns `lib/shopify/ingest.ts` — the tenant-scoped upsert mappers that webhooks (Plan 04) and the nightly reconcile (Plan 05) reuse. It turns the encrypted token from Plan 02 into real rows.
Output: Beauty Square's real products + orders + on_hand inventory in the DB and on the dashboard; mock client gone.

**Human checkpoint (destructive):** The synthetic->real swap deletes Beauty Square's 1023 synthetic Products/SalesHistory/Predictions/Orders. It runs ONLY after a dry-run count is surfaced and the owner explicitly confirms (D-13 + Roy's "ask before destructive ops" rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-shopify-ingest-odoo/03-CONTEXT.md
@.planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md
@.planning/phases/03-real-shopify-ingest-odoo/03-02-SUMMARY.md

<interfaces>
<!-- From lib/shopify/shopify.ts (Plan 02) — reconstruct an offline session to build a GraphQL client -->
export const shopify;  // shopifyApi singleton, ApiVersion.April26, offline
// GraphQL client: new shopify.clients.Graphql({ session })  (session rebuilt from ShopifyConnection: shop + decrypted token, isOnline:false)

<!-- From lib/crypto/encryption.ts (Plan 01) -->
export function decrypt(payload: string): string;   // decrypt ShopifyConnection.accessToken to rebuild the session

<!-- From lib/auth/route-wrapper.ts (Phase 2) -->
export async function requireTenantOrResponse(slug?): Promise<TenantContext | NextResponse>;

<!-- Prisma models (Plan 01): Location { @@unique([tenantId, shopifyLocationId]) }, InventoryLevel { onHand Int, @@unique([locationId, productId]) }, IngestCursor { @@unique([tenantId, source, resource]) } -->
<!-- Product @@unique([tenantId, shopifyProductId]); SalesHistory @@unique([productId, date, channel]) -->

<!-- The forecast run route (app/api/forecast/run/route.ts) is re-triggered after cutover to recompute against real data. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSONL parser (__parentId reassembly) with a fixture test + Bulk Operations launcher/poller</name>
  <files>lib/shopify/jsonl.ts, lib/shopify/jsonl.test.ts, lib/shopify/bulk.ts</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Pattern 3 — Bulk Operations" lines 61-85 (the orders/products GraphQL query, the currentBulkOperation poll loop, the JSONL-with-__parentId flattening) + §Pitfalls #4 (one bulk op per shop — serialize) #5 (JSONL is flat with __parentId)
    - 03-RESEARCH.md §"Validation Architecture" — "JSONL parser test: a fixture JSONL with __parentId nesting -> assert orders/lineItems reassemble correctly"
    - vitest.config.ts (test glob lib/**/*.test.ts)
  </read_first>
  <behavior>
    - parseBulkJsonl(fixture) where fixture has an order line then two lineItem lines carrying `__parentId` = the order's id -> returns one order object with a `lineItems` array of length 2
    - A line with no `__parentId` is treated as a top-level (parent) record
    - A line whose `__parentId` references an unseen parent is tolerated (skipped or buffered) without throwing
    - Empty input -> returns an empty array (no throw)
    - The parser is line-oriented (splits on newlines, JSON.parse per non-empty line) so it can stream a large file
  </behavior>
  <action>
    1. `lib/shopify/jsonl.ts` — export `parseBulkJsonl(text: string): ParentRecord[]`. Per RESEARCH Pitfall 5: Bulk JSONL is FLAT — each line is one JSON object; children (lineItems, variants, inventoryLevels) are SEPARATE lines that carry `__parentId` pointing at their parent's `id`. Algorithm: iterate lines, JSON.parse each non-empty line; if the object has no `__parentId`, register it as a parent keyed by its `id`; if it has `__parentId`, push it onto the parent's children bucket (infer the child collection name from the child's `id` gid type, e.g. `.../LineItem/...` -> `lineItems`, or accept an explicit mapping arg). Return the assembled parents in first-seen order. Tolerate orphans (buffer or skip, never throw).
    2. `lib/shopify/jsonl.test.ts` — implement the five behaviors with an inline fixture string (one order + two lineItems referencing it via `__parentId`).
    3. `lib/shopify/bulk.ts` — export `runBulkQuery(client, graphqlQueryString)` that: (a) calls `bulkOperationRunQuery` with the query, checks `userErrors` is empty; (b) polls `{ currentBulkOperation { id status errorCode objectCount url } }` every ~2s until `status === "COMPLETED"` (or throws on `FAILED`/errorCode); (c) fetches the returned `url` (temporary GCS link) and returns its text body. Add a guard/comment: only ONE bulk op runs per shop at a time, so callers MUST serialize orders -> products -> inventory (Pitfall 4). Include the exact orders query (`orders(query: "created_at:>=<365d-ago>")` with lineItems{quantity sku product{id} variant{id} originalUnitPriceSet}) and the inventory query using `quantities(names: ["on_hand"])` from RESEARCH lines 78-85 as exported query-string constants.
  </action>
  <verify>
    <automated>npx vitest run lib/shopify/jsonl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/shopify/jsonl.test.ts` exits 0 (all five behaviors pass)
    - `grep "__parentId" lib/shopify/jsonl.ts` matches
    - `grep "bulkOperationRunQuery" lib/shopify/bulk.ts` matches and `grep "currentBulkOperation" lib/shopify/bulk.ts` matches
    - `grep 'quantities(names:' lib/shopify/bulk.ts` matches (on_hand inventory query, SHOP-04) and `grep -w "available" lib/shopify/bulk.ts` returns NOTHING
    - `grep -i "one bulk\|serialize\|one operation" lib/shopify/bulk.ts` matches (the serialize-per-shop guard is documented)
  </acceptance_criteria>
  <done>JSONL parser reassembles __parentId children (fixture test green); bulk runner launches+polls+fetches; inventory query uses on_hand.</done>
</task>

<task type="auto">
  <name>Task 2: Ingest mappers (lib/shopify/ingest.ts) + backfill route that runs the three bulk ops and upserts</name>
  <files>lib/shopify/ingest.ts, app/api/shopify/backfill/route.ts</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Pattern 3" (orders/products/inventory bulk queries; Location -> primary = first active location, set isPrimary) + §Open Question #4 (variant granularity — inspect Beauty Square's first ingest: if products are single-variant, key InventoryLevel on Product; the mapper keys on the @@unique([locationId, productId]))
    - lib/shopify/bulk.ts + lib/shopify/jsonl.ts (Task 1 — runBulkQuery + parseBulkJsonl + the query constants)
    - lib/shopify/shopify.ts (build the GraphQL client from a rebuilt offline session) + lib/crypto/encryption.ts (decrypt the stored token)
    - prisma/schema.prisma (Product @@unique([tenantId, shopifyProductId]); SalesHistory @@unique([productId, date, channel]); Location @@unique([tenantId, shopifyLocationId]); InventoryLevel @@unique([locationId, productId]))
    - app/api/forecast/run/route.ts (the SalesHistory shape these orders map into: { tenantId, productId, date, quantity, revenueKes, channel:"shopify" })
  </read_first>
  <action>
    1. `lib/shopify/ingest.ts` — export tenant-scoped, idempotent upsert mappers (every query carries tenantId — tenant-safety ESLint applies). These are REUSED by webhooks (Plan 04) and reconcile (Plan 05), so keep them pure (take a `tenantId` + a normalized Shopify node, do one upsert each):
       - `upsertProductFromShopify(tenantId, node)` -> `prisma.product.upsert({ where: { tenantId_shopifyProductId: { tenantId, shopifyProductId } }, create/update: { sku, title, vendor, productType, priceKes, shopifyVariantId, imageUrl, ... } })`. Map real `productType` straight through (RESEARCH/D-08: real values won't match kenya-calendar.ts uppercase set — that's a calibration item, tolerate it; do not crash).
       - `upsertLocationFromShopify(tenantId, node, { isPrimary })` -> upsert on `tenantId_shopifyLocationId`; set isPrimary for the FIRST active location.
       - `upsertInventoryLevel(tenantId, locationId, productId, onHand)` -> upsert on `locationId_productId`, write `onHand` (the on_hand number).
       - `upsertOrderAsSales(tenantId, orderNode)` -> for each line item, upsert a SalesHistory row keyed on `productId_date_channel` (channel "shopify"), quantity + revenueKes from the line; resolve the local productId by the line's shopify product id (skip lines whose product was not ingested).
    2. `app/api/shopify/backfill/route.ts` (POST): `requireTenantOrResponse()`; load `ShopifyConnection` for the tenant, decrypt the token, rebuild an offline session, `new shopify.clients.Graphql({ session })`. Then, SERIALIZED (one bulk op per shop): (a) run the LOCATIONS+inventory query, parse, upsert locations (mark first active primary) + inventory on_hand; (b) run the PRODUCTS bulk op, parse, upsert products/variants; (c) run the 365d ORDERS bulk op (`created_at:>=<today-365d>`), parse, upsert SalesHistory. Return `{ ok: true, products: P, orders: O, locations: L, inventoryLevels: I }` counts. This route is also the data source for the cutover dry-run in Task 3 (it can run in a "count only" mode, or Task 3 reads the resulting counts).
    Keep ingest writes as DIRECT tenant-scoped upserts (Phase 4 retrofits them through applyClaim — out of scope here, per 03-CONTEXT non-scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "lib/shopify/ingest|shopify/backfill" || echo "ingest + backfill typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export (async )?function (upsertProductFromShopify|upsertLocationFromShopify|upsertInventoryLevel|upsertOrderAsSales)" lib/shopify/ingest.ts` returns all four mappers
    - `grep "tenantId_shopifyProductId" lib/shopify/ingest.ts` matches (upsert on the composite unique key)
    - `grep "isPrimary" lib/shopify/ingest.ts` matches (first active location marked primary, SHOP-08)
    - `grep "onHand" lib/shopify/ingest.ts` matches and `grep -w "available" lib/shopify/ingest.ts` returns NOTHING
    - `grep "shopify.clients.Graphql" app/api/shopify/backfill/route.ts` matches and `grep "decrypt(" app/api/shopify/backfill/route.ts` matches
    - `npx tsc --noEmit` reports no errors in ingest.ts or the backfill route
  </acceptance_criteria>
  <done>Four reusable tenant-scoped mappers exist; backfill route serially runs locations->products->orders bulk ops and upserts real data including on_hand inventory and primary location.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Guarded synthetic-to-real cutover (dry-run count + owner confirm)</name>
  <what-built>The backfill route (Task 2) pulls Beauty Square's real catalog, 365d orders, and on_hand inventory into staging upserts. The GUARDED synthetic->real cutover is the destructive step that this checkpoint authorizes.</what-built>
  <how-to-verify>
    The cutover (D-13) is destructive and MUST be owner-confirmed. Implement + run it as part of this checkpoint:
    1. **Dry-run first (no deletes):** trigger backfill in count mode and surface: "This will REPLACE N synthetic products + their sales/predictions/orders with M real products from Shopify. Suppliers, promos, and monthly context are preserved. Confirm?" — show the actual N (current synthetic count) and M (real count from the bulk ops). Roy reviews the numbers.
    2. **On explicit confirm only**, run the cutover transaction (Task 2's mappers + a guarded delete) — see Task wiring: in a per-tenant `prisma.$transaction`, `deleteMany` synthetic `Order` -> `Prediction` -> `SalesHistory` -> `Product` for that tenantId (child-first order to respect FKs), then upsert the real products/inventory/orders. KEEP Supplier/Promo/MonthlyContext (owner-entered, not synthetic — RESEARCH Pattern 6).
    3. **Re-run forecasts** against real data: POST `/api/forecast/run` for the tenant.
    4. Verify in the dashboard at `/shop/beauty-square/dashboard`: real product titles/vendors appear (not the synthetic catalog), and inventory reflects on_hand.
    Confirm counts post-cutover:
    `npx tsx -e "import {prisma} from './lib/prisma'; Promise.all([prisma.product.count(),prisma.salesHistory.count(),prisma.location.count(),prisma.inventoryLevel.count(),prisma.supplier.count()]).then(([p,s,l,i,sup])=>{console.log({products:p,sales:s,locations:l,inventory:i,suppliers:sup});process.exit(0)})"`
    Expect: products = real M (not 1023 synthetic), locations >= 1, inventoryLevels >= 1, suppliers unchanged (preserved).
  </how-to-verify>
  <resume-signal>Type "cutover confirmed" AFTER reviewing the dry-run counts and verifying real data renders. If the numbers look wrong, type "abort" and NO delete runs.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete the mock ShopifyClient, fix its only consumer, remove the ESLint allow-list entry</name>
  <files>lib/shopify/client.ts, app/api/shop/test/route.ts, eslint.config.mjs</files>
  <read_first>
    - lib/shopify/client.ts (the mock being deleted — its ONLY production consumer is app/api/shop/test/route.ts which calls new ShopifyClient(...).testConnection(); fetchProducts/fetchOrders/fetchInventory/createDraftOrder are unused by any live route)
    - app/api/shop/test/route.ts (rewrite or remove — see action)
    - eslint.config.mjs lines 26-31 (the `"lib/shopify/client.ts"` entry in the tenant-safety `ignores` array — remove it; the comment says "TODO(Phase 3): delete with the mock")
    - .planning/phases/03-real-shopify-ingest-odoo/03-CONTEXT.md D-13/code_context (replace the mock wholesale; remove the allow-list entry)
  </read_first>
  <action>
    1. Delete `lib/shopify/client.ts` entirely (the real OAuth + GraphQL ingest now exists; the mock's prisma.tenant.findFirst() stubs were always throwaway).
    2. Fix its only consumer, `app/api/shop/test/route.ts`: it currently imports `ShopifyClient` and calls `testConnection()`. Replace with a real, tenant-scoped connection check: `requireTenantOrResponse()`, load the tenant's `ShopifyConnection` (findUnique by tenantId), and return `{ ok: true, connected: <row exists && uninstalledAt==null>, shopName: <shopDomain or "not connected"> }`. Do NOT decrypt or expose the token. (Alternatively, if simpler and acceptable, delete this legacy "test connection" route and remove its button usage — but the connection status already lives at /api/shopify/connection from Plan 02, so converting this route to a thin real check OR deleting it are both fine; pick one and ensure no dangling import remains.)
    3. Remove the `"lib/shopify/client.ts"` line (and its now-stale comment block) from the `ignores` array in `eslint.config.mjs`'s tenant-safety config object. Leave the other allow-list entries (context.ts, webhook-context.ts, onboarding, page.tsx) intact.
    4. Run the lint gate to prove nothing references the deleted mock and the allow-list removal didn't break the build: `npm run lint`.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `lib/shopify/client.ts` does NOT exist (file deleted)
    - `grep -rn "shopify/client\|ShopifyClient" app lib` returns NOTHING (no dangling imports of the mock)
    - `grep "lib/shopify/client.ts" eslint.config.mjs` returns NOTHING (allow-list entry removed)
    - `npm run lint` exits 0 (tenant-safety gate passes with the mock gone and the allow-list entry removed)
    - `npx tsc --noEmit` reports no errors (app/api/shop/test/route.ts compiles without the mock import)
  </acceptance_criteria>
  <done>Mock client deleted, its consumer rewired to a real tenant-scoped check (or removed), ESLint allow-list entry gone, lint + typecheck green.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/shopify/jsonl.test.ts` green (JSONL __parentId reassembly).
- Backfill renders Beauty Square's real catalog + 365d orders + on_hand inventory; Location.isPrimary set (SHOP-03/04/08, success criterion #2).
- Cutover was guarded (dry-run + confirm) and preserved Supplier/Promo/MonthlyContext; forecasts re-ran on real data.
- Mock client deleted; `npm run lint` passes with the allow-list entry removed.
</verification>

<success_criteria>
- Real products/variants + 365d orders + on_hand inventory ingested via Bulk Operations.
- Location first-class with primary set; InventoryLevel.onHand populated (never `available`).
- D-13 synthetic->real swap executed only after a surfaced dry-run count + explicit owner confirmation.
- Mock ShopifyClient removed and de-allow-listed; lint + typecheck clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-shopify-ingest-odoo/03-03-SUMMARY.md` (record the real product/order/location counts, the variant-granularity finding for OQ4, and confirm the ingest mappers' signatures for Plans 04/05).
</output>

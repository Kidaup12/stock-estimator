---
phase: 03-real-shopify-ingest-odoo
plan: 05
type: execute
wave: 5
depends_on: ["03-03", "03-04"]
files_modified:
  - lib/shopify/reconcile.ts
  - app/api/cron/shopify-reconcile/route.ts
  - vercel.json
  - lib/shopify/uninstall.test.ts
autonomous: true
requirements: [SHOP-07, SHOP-09]
must_haves:
  truths:
    - "Forcing a webhook miss (edit in Shopify while the handler is down) and running the reconcile cron catches the delta via IngestCursor"
    - "The reconcile route is guarded by Authorization: Bearer CRON_SECRET and is callable manually locally with the same header"
    - "vercel.json schedules the reconcile at 02:00 UTC nightly"
    - "Uninstall clears ShopifyConnection token + sets uninstalledAt while preserving the tenant's Product/SalesHistory/Supplier rows (SHOP-09) — proven by a test"
  artifacts:
    - path: "app/api/cron/shopify-reconcile/route.ts"
      provides: "Bearer-guarded nightly delta sweep over connected tenants using IngestCursor"
      exports: ["GET"]
    - path: "lib/shopify/reconcile.ts"
      provides: "Per-tenant delta sweep: read cursor -> query orders/products updated_at>=cursor -> upsert -> advance cursor"
      exports: ["reconcileTenant"]
    - path: "vercel.json"
      provides: "Vercel Cron schedule for /api/cron/shopify-reconcile"
      contains: "shopify-reconcile"
  key_links:
    - from: "app/api/cron/shopify-reconcile/route.ts"
      to: "process.env.CRON_SECRET"
      via: "Authorization: Bearer guard"
      pattern: "CRON_SECRET"
    - from: "lib/shopify/reconcile.ts"
      to: "IngestCursor @@unique([tenantId, source, resource])"
      via: "read cursor, query updated_at>=cursor, advance to now"
      pattern: "ingestCursor"
    - from: "lib/shopify/reconcile.ts"
      to: "lib/shopify/ingest.ts mappers"
      via: "delta rows upserted through the same upsert functions"
      pattern: "upsert.*FromShopify|upsertOrderAsSales"
---

<objective>
Close the durability gap: a nightly reconcile cron that does a delta sweep to catch any webhook misses (SHOP-07) using `IngestCursor` for resumability, scheduled via Vercel Cron and callable manually locally; plus a test proving the `app/uninstalled` cleanup clears tokens while preserving tenant data (SHOP-09). This is the safety net that makes the webhook-primary design (D-09) trustworthy even when Shopify can't deliver (the Kenya 2-day-outage scenario).

Purpose: Webhooks (Plan 04) are best-effort; reconcile guarantees eventual consistency. IngestCursor makes each sweep resumable and incremental (only `updated_at >= cursor`).
Output: A Bearer-guarded reconcile route + vercel.json schedule + an uninstall-preservation test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-shopify-ingest-odoo/03-CONTEXT.md
@.planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md
@.planning/phases/03-real-shopify-ingest-odoo/03-03-SUMMARY.md
@.planning/phases/03-real-shopify-ingest-odoo/03-04-SUMMARY.md

<interfaces>
<!-- From lib/shopify/ingest.ts (Plan 03) — reuse for delta upserts -->
export function upsertProductFromShopify(tenantId: string, node): Promise<...>;
export function upsertOrderAsSales(tenantId: string, orderNode): Promise<...>;

<!-- From lib/shopify/shopify.ts (Plan 02) + lib/crypto/encryption.ts (Plan 01) -->
export const shopify;                                 // GraphQL client per connected tenant
export function decrypt(payload: string): string;     // decrypt ShopifyConnection.accessToken

<!-- Prisma (Plan 01): IngestCursor { tenantId, source, resource, cursor DateTime?, @@unique([tenantId, source, resource]) } -->
<!-- ShopifyConnection { tenantId @unique, accessToken, uninstalledAt } — iterate where uninstalledAt == null -->

<!-- The app/uninstalled topic handler (Plan 04, lib/shopify/webhooks.ts) already nulls the token + sets uninstalledAt; this plan adds the regression TEST that it preserves data. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-tenant reconcile delta sweep (lib/shopify/reconcile.ts)</name>
  <files>lib/shopify/reconcile.ts</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Pattern 5 — Nightly reconcile" lines 113-114 (read IngestCursor per resource, query orders(query:"updated_at:>=<cursor>") + products(query:"updated_at:>=<cursor>"), upsert deltas, advance the cursor to now — catches webhook misses, SHOP-07)
    - lib/shopify/ingest.ts (Plan 03 — upsertProductFromShopify / upsertOrderAsSales; reuse)
    - lib/shopify/bulk.ts (Plan 03 — if the delta volume is large, the same bulk-op pattern can be reused; for incremental sweeps a normal paginated GraphQL query is usually enough — pick per RESEARCH)
    - lib/shopify/shopify.ts + lib/crypto/encryption.ts (build the GraphQL client from the decrypted offline token)
    - prisma/schema.prisma IngestCursor (@@unique([tenantId, source, resource]))
  </read_first>
  <action>
    `lib/shopify/reconcile.ts` — export `reconcileTenant(tenantId: string)`:
    1. Load `ShopifyConnection` for the tenant; if missing or `uninstalledAt != null`, skip (return a no-op summary). Decrypt the token, rebuild the offline session, `new shopify.clients.Graphql({ session })`.
    2. For each resource in `["products", "orders"]`:
       - Read `IngestCursor` via `prisma.ingestCursor.findUnique({ where: { tenantId_source_resource: { tenantId, source: "shopify", resource } } })`. Use `cursor` as the `updated_at` floor; if absent, default to a sensible window (e.g. last 7 days, since the first-connect backfill already loaded 365d).
       - Query Shopify for that resource with `query: "updated_at:>=<cursor ISO>"`, paginating until exhausted. Upsert each node via the Plan 03 mappers (idempotent — re-applying a row a webhook already handled is harmless).
       - Advance the cursor: `prisma.ingestCursor.upsert({ where: { tenantId_source_resource: {...} }, create/update: { cursor: <sweepStartedAt> } })`. Use the timestamp captured at the START of the sweep (not end) to avoid missing rows changed mid-sweep.
    3. Return `{ products: P, orders: O }` delta counts.
    Keep it tenant-scoped (tenant-safety ESLint applies — every query carries tenantId).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "lib/shopify/reconcile" || echo "reconcile module typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export (async )?function reconcileTenant" lib/shopify/reconcile.ts` matches
    - `grep "ingestCursor" lib/shopify/reconcile.ts` matches and `grep "updated_at" lib/shopify/reconcile.ts` matches (delta floor, SHOP-07)
    - `grep "tenantId_source_resource" lib/shopify/reconcile.ts` matches (cursor keyed on the composite unique)
    - `grep -E "upsert.*FromShopify|upsertOrderAsSales" lib/shopify/reconcile.ts` matches (reuses Plan 03 mappers, no duplicate upsert logic)
    - `grep "uninstalledAt" lib/shopify/reconcile.ts` matches (skips uninstalled connections)
    - `npx tsc --noEmit` reports no errors in reconcile.ts
  </acceptance_criteria>
  <done>reconcileTenant reads each resource's IngestCursor, sweeps updated_at>=cursor deltas through the Plan 03 mappers, and advances the cursor to the sweep-start timestamp.</done>
</task>

<task type="auto">
  <name>Task 2: Bearer-guarded cron route + vercel.json schedule</name>
  <files>app/api/cron/shopify-reconcile/route.ts, vercel.json</files>
  <read_first>
    - .planning/phases/03-real-shopify-ingest-odoo/03-RESEARCH.md §"Pattern 5" lines 113-114 (vercel.json crons entry { path:"/api/cron/shopify-reconcile", schedule:"0 2 * * *" }; route guards on Authorization: Bearer ${CRON_SECRET}; local manual trigger with the same header: curl -H "Authorization: Bearer $CRON_SECRET" localhost:3082/api/cron/shopify-reconcile)
    - lib/shopify/reconcile.ts (Task 1 — reconcileTenant)
    - .env.example (CRON_SECRET added in Plan 02 Task 1 — this route consumes it)
    - CLAUDE.md (Next 16 route handler conventions; :3082 dev port)
    - Check whether vercel.json already exists (it does NOT at plan time — create it fresh; if Plan work added one, MERGE the crons array rather than overwrite)
  </read_first>
  <action>
    1. `app/api/cron/shopify-reconcile/route.ts` (GET — Vercel Cron issues GET):
       - Guard: `const auth = req.headers.get("authorization"); if (auth !== \`Bearer ${process.env.CRON_SECRET}\`) return new NextResponse("Unauthorized", { status: 401 });`. This is NOT a session route (it runs headless on a schedule) — it is intentionally outside requireTenant; the Bearer secret is its auth. (It does not do bare tenant queries that the ESLint rule would flag — it iterates ShopifyConnection by uninstalledAt and calls reconcileTenant per tenantId; if the linter flags the connection scan, it is a headless system route — handle by keeping the query keyed/explicit, not by session.)
       - Load all connected tenants: `prisma.shopifyConnection.findMany({ where: { uninstalledAt: null } })`. For each, `await reconcileTenant(conn.tenantId)`, collecting counts. Wrap each tenant in try/catch so one tenant's failure doesn't abort the sweep.
       - Return `{ ok: true, tenants: N, results: [...] }`.
       - `export const maxDuration = 300;` (the sweep can be slow) and `export const runtime = "nodejs";`.
    2. `vercel.json` (create at repo root): `{ "crons": [{ "path": "/api/cron/shopify-reconcile", "schedule": "0 2 * * *" }] }` (02:00 UTC nightly). If a vercel.json already exists, add this object to the existing `crons` array instead of overwriting the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "cron/shopify-reconcile" || node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8')); console.log('vercel.json valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CRON_SECRET" app/api/cron/shopify-reconcile/route.ts` matches and `grep -i "Bearer" app/api/cron/shopify-reconcile/route.ts` matches (auth guard)
    - `grep "status: 401" app/api/cron/shopify-reconcile/route.ts` matches (unauthorized rejected)
    - `grep "reconcileTenant" app/api/cron/shopify-reconcile/route.ts` matches and `grep "uninstalledAt: null" app/api/cron/shopify-reconcile/route.ts` matches (only connected tenants)
    - `node -e "const c=require('./vercel.json'); const ok=c.crons.some(x=>x.path==='/api/cron/shopify-reconcile' && x.schedule==='0 2 * * *'); process.exit(ok?0:1)"` exits 0
    - `npx tsc --noEmit` reports no errors in the cron route
  </acceptance_criteria>
  <done>Cron route rejects requests without the Bearer secret, sweeps every connected tenant via reconcileTenant, and vercel.json schedules it nightly at 02:00 UTC.</done>
</task>

<task type="auto">
  <name>Task 3: Uninstall data-preservation regression test (SHOP-09)</name>
  <files>lib/shopify/uninstall.test.ts</files>
  <read_first>
    - lib/shopify/webhooks.ts (Plan 04 — the app/uninstalled handler that nulls ShopifyConnection.accessToken + sets uninstalledAt and PRESERVES Product/SalesHistory/etc.; this test exercises that handler or its extracted helper)
    - tests/tenant-isolation.test.ts (the existing Prisma-direct integration-test pattern: seed a uniquely-namespaced tenant against the real DATABASE_URL, assert, strict teardown via deleteMany on a slug prefix) — REPLICATE this harness style
    - .planning/phases/03-real-shopify-ingest-odoo/03-CONTEXT.md D-10 (uninstall clears tokens but preserves tenant data)
    - vitest.config.ts (lib/**/*.test.ts glob)
  </read_first>
  <action>
    `lib/shopify/uninstall.test.ts` — an integration test mirroring `tests/tenant-isolation.test.ts` style (Prisma-direct, real DB, namespaced teardown):
    1. In setup, create a throwaway tenant (slug prefix like `uninstall-test-`) with: a `ShopifyConnection` (tenantId, shopDomain, accessToken = some ciphertext-looking string, uninstalledAt = null), one `Product`, and one `SalesHistory` row.
    2. Invoke the uninstall path: either call the exported `app/uninstalled` handler from `lib/shopify/webhooks.ts` (if `dispatch("app/uninstalled", tenantId, {})` is callable in-process) OR call the extracted uninstall helper. (If the uninstall logic is inline in dispatch and not separately exported, extract a small `handleUninstall(tenantId)` in lib/shopify/webhooks.ts and call that — keeps the test honest.)
    3. Assert AFTER uninstall:
       - `ShopifyConnection.accessToken` is null AND `uninstalledAt` is set (token cleared).
       - The `Product` row STILL exists (count for the tenant unchanged).
       - The `SalesHistory` row STILL exists.
    4. Teardown: `prisma.tenant.deleteMany({ where: { slug: { startsWith: "uninstall-test-" } } })` (cascade wipes children).
  </action>
  <verify>
    <automated>npx vitest run lib/shopify/uninstall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/shopify/uninstall.test.ts` exits 0
    - The test asserts `accessToken` becomes null and `uninstalledAt` is set (grep the test for `toBeNull` on accessToken and a truthy uninstalledAt assertion)
    - The test asserts the Product and SalesHistory rows survive (grep for product.count / salesHistory.count or findUnique still-truthy assertions post-uninstall)
    - `grep "uninstall-test-" lib/shopify/uninstall.test.ts` matches (namespaced teardown, matches the isolation-test convention)
  </acceptance_criteria>
  <done>A regression test proves app/uninstalled nulls the token + sets uninstalledAt while leaving Product/SalesHistory intact — SHOP-09 locked.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean for reconcile + cron route.
- vercel.json schedules /api/cron/shopify-reconcile at 02:00 UTC; the route 401s without the Bearer secret and sweeps connected tenants via IngestCursor (SHOP-07).
- `npx vitest run lib/shopify/uninstall.test.ts` green — uninstall clears token, preserves data (SHOP-09).
- Manual local proof (optional, in checkpoint-free flow): `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3082/api/cron/shopify-reconcile` returns `{ ok: true, tenants: N }`.
</verification>

<success_criteria>
- Nightly reconcile catches webhook misses via IngestCursor delta sweep, resumable per resource.
- Cron route Bearer-guarded; vercel.json schedules it nightly.
- Uninstall preserves tenant data while clearing tokens (tested).
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-shopify-ingest-odoo/03-05-SUMMARY.md`. This is the final Shopify plan — note in the SUMMARY that ODOO-01..05 remain DEFERRED (D-12) to a Phase 3 tail / later milestone (no live Odoo test target + ODOO-02/03 depend on the Phase 4 merge layer).
</output>
